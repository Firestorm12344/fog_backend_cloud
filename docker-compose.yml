version: '3.9'

services:
  fog-backend:
    build:
      context: .
    container_name: fog-backend
    ports:
      - "5050:5050"
    environment:
      PORT: 5050
      HOST: 0.0.0.0
    restart: unless-stopped
