from preprocessing import preprocess

model = None


def run_inference(signals):
    """Stub de inferencia para despliegue web.
    Reemplazar con carga real del modelo `.h5` y la lógica de predicción.
    """
    return {
        "probability": 0.0,
        "class": "No FOG",
    }
